"""FastAPI routers split out of web/app.py (v0.6.8.5).

This package only holds route definitions; business logic stays in the
existing modules under web/. See web/app.py for app initialization,
middleware, and non-auth/non-admin routes.
"""
