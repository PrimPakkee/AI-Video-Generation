# AI Video Generation Requirements

# OpenAI-compatible API client
openai>=1.0.0

# Environment variable management (optional but recommended)
python-dotenv>=1.0.0

# Web framework (v0.3+)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Database (v0.3+)
SQLAlchemy>=2.0.0

# HTTP client used by ApxSeedanceProvider (v0.6.0+)
requests>=2.31.0

# Pillow (v0.6.3+) — used by web/image_video_pipeline.py to draw the
# educational slide PNGs that FFmpeg then concatenates into final_video.mp4.
# The Image Video MVP renders text and shapes locally; no remote Image2 /
# Seedance / TTS calls are made.
Pillow>=10.0.0

# v0.6.8 — auth (login/register + admin approval)
# bcrypt: password hashing for the users table
# itsdangerous: required by Starlette's SessionMiddleware for the
# HttpOnly session cookie
bcrypt>=4.0.0
itsdangerous>=2.1.0
